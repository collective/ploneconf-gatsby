import React from 'react';
import { array, object, string } from 'prop-types';

const TrainersList = data => {
  console.log(data);
  return <React.Fragment>prova</React.Fragment>;
};

TrainersList.propTypes = {
  data: object,
};

export default TrainersList;
